1) Create the smart contract (such as the DumbDonuts.sol)
2) Add dependencies (yarn add all)
yarn add atomize react-transition-group
3) Go to terminal and type ganache-cli
4) Set up test network on metamask and copy one of the private keys in the terminal. import the private key into metamask
5) truffle migrate --reset || truffle test
6) test by running yarn start || control c

git init
git remote add origin ...
git status
git add .
git commit -m "commit"
npm run deploy
git push -u origin master

